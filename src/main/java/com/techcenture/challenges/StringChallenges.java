package com.techcenture.challenges;

public class StringChallenges {

    /*
    Given a string name, e.g. "Bob", return a greeting of the form "Hello Bob!".
     */
    public String helloName(String name) {
        return null;
    }

    /*
    Given a string, return a version without the first and last char,
    so "Hello" yields "ell". The string length will be at least 2.
     */
    public String withoutEnd(String str) {
        return null;
    }

    /*
    Given 2 strings, a and b, return a string with greater length,
     */
    public String returnLarger(String a, String b) {
        return null;
    }

    /*
    Given 2 strings, return their concatenation, except omit the first char of each.
    The strings will be at least length 1.
     */
    public String nonStart(String a, String b) {
        return null;
    }

    /*
    Given a string, return a "rotated left 2" version where the first 2 chars are moved to the end.
    The string length will be at least 2.
     */
    public String left2(String str) {
        return null;
    }

    /*
    Given a string, return a version without the first 2 chars.
    Except keep the first char if it is 'a' and keep the second char if it is 'b'.
    The string may be any length. Harder than it looks.
     */
    public String deFront(String str) {
        return null;
    }


    /*
    Given a string, return a string where for every char in the original,
    there are two chars.
     */
    public String doubleChar(String str) {
      return null;
    }

    //Return the number of times that the string "hi"
    // appears anywhere in the given string.
    public int countHi(String str) {
       return 0;
    }

    //Return true if the string "java" and "python" appear the same number of
    // times in the given string.
    public boolean javaPython(String str) {
       return false;
    }

    /*
    Given two strings, return true if either of the strings appears at the very
    end of the other string, ignoring upper/lower case differences (in other words,
    the computation should not be "case sensitive")
     */
    public boolean endOther(String a, String b) {
        return false;
    }


}
